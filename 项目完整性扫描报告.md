# 项目完整性扫描报告

> **项目**：Pi-Agent（GOAI 方向三 · 材料科学文献驱动的科学发现智能体 · 路线 A 构效关系发现）
> **扫描时间**：2026-08-16
> **扫描方式**：5 个只读子代理分维度扫描（赛题合规 / 提交物完整性 / 文档一致性 / 证据链 / 代码质量）+ 关键结论人工复核
> **说明**：本报告所有"已确认"结论均经过人工二次核验（直接读文件/跑命令），非子代理转述；标注"子代理声称"的项为待人工复核项。

---

## 一、本次已完成的更新

| 文件 | 改动 |
|---|---|
| `docs/CROSS_THEME_REPORT.md` | 2 处"discovery_report 待生成"→"已生成"（P2 行动项 L296 + 7.3 数据完整性 L317） |

---

## 二、整体评价

**赛题合规 ✅ 基本满足**：基本任务四项能力（文献检索筛选 / 知识抽取 / Research Gap 识别 / 调研报告生成）、路线 A 四考察点（搜索算法 / LLM 融合 / 可信性与新颖性 / 领域理解与可解释性）、复赛七项提交物（可运行代码 / 实验结果报告 / 科学意义阐释 / 依赖披露等）均有真实运行产物支撑；合规披露（商业 API / 闭源模型 / 数据来源授权 / 密钥管理）逐项覆盖且诚实度高（包括对数据不足、间接证据的如实标注）。

项目在 **"流程完整、证据可审计、披露诚实"** 三个维度达到竞赛级准备。但扫描发现 **2 处会被评审当场抓住的硬伤 + 若干工程漏洞**（均为人工复核确认）。

---

## 三、已确认漏洞（按严重度排序）

### 🔴 P0-1：SP 清单 Best Score 全为 0.000，与所有其他文档矛盾

- **现象**：`路线A_构效关系清单与解释文档/ROUTE_A_SP_LIST.md` 全部 31 条 Best Score = 0.000（L59/81/101/123/143/165/185/205/225/245/265/285/305/325/345/365/385/405/430/450/474/494/514/534/554/574/594/614/638/658/678）
- **对照**：`search_h*.json` 中有真实分数 0.66–0.91（主案例 search_h0=0.665 / h1=0.829 / h2=0.677 / h3=0.913 / h4=0.734）；`CROSS_THEME_REPORT.md` 表格 = 0.66~0.91；README / 方案文档 = 0.665–0.913
- **根因**：`scripts/build_route_a_docs.py:155` 从 `hypotheses.json` 的 `best_score` 字段读取，但搜索分数存在 `search_h*.json` 中**从未回填**到 hypotheses.json（主案例 5 条全为 0.0/None，cathode/thermoelectric/perovskite/mof_e2e_v4 的 hypotheses[0] 也全是 0.0）
- **影响**：评审交叉核对提交文档时会立刻发现矛盾——数据链路缺陷而非造假，但观感与可信度受损严重

### 🔴 P0-2：SP 清单含占位文本，触碰"新知与已知分清"硬约束

- 主案例 SPR-MOF-01~05「已知工作」= **"具体结论需人工/LLM 补写"**、「增量贡献」= **"相对已确立结论的具体增量待补写"**（`ROUTE_A_SP_LIST.md` L65-66/87-88/107-108/129-130/149-150）
- MOFv4 多条「证据链」= **"（证据链条目存在，但需清理格式）"**（L170/190/210/230/250）
- **影响**：提交的主清单上仍有未完成字段，与 2026-08-16 已完成的 hypotheses.json 侧补写（known_prior_work / incremental_claim 已锚定 P#）不同步——SP 清单未重新生成

### 🔴 P0-3：主案例证据链 p# 编号系统性错位（语义级）

- **现象**：`文献调研报告/主案例_MOF-CO2/report.tex` 正文 `\cite{p65}` 语境为 **"CoMn-MOF-74 双金属 1:1 最佳"**，但 `references.bib` 中 p65 实际条目是 **"One pot synthesis of UiO-66@IL composite…"（Chemosphere 2022, DOI 10.1016/j.chemosphere.2022.135122）**——完全不同的论文
- 同类错位：`\cite{p67}`（正文 Fe/Cu-MOF 双金属）→ bib p67 为 ZIF-8 氨基 MMM（Sep. Purif. Technol. 2022）；`\cite{p7}`（正文热电/热力学筛选语境）→ bib p7 为 Thermodynamic screening of metal-substituted MOFs
- **规模**：子代理统计约 20 个 key 语义错位（p65/p67/p22/p24/p147/p27/p129/p31/p50/p35/p60/p17/p62/p169/p13/p7/p20/p116/p117/p118/p128/p168/p176/p178 等）+ bib 内 3 组重复条目（p116=p210、p182=p30、p147=p188，DOI 完全相同）+ 占位条目（p189/p193/p197/p206 及 5 个 r10/r11 条目标注"无 DOI/独立元数据"）无法外部核验
- **关键事实**：bib 条目数与 report.tex 的 `\cite` key 数量一一对应（71=71），但**"数量对得上"是假象——语义全错位**；report.log 存在 `LaTeX Warning: There were undefined references` + `Please (re)run BibTeX` 警告
- **影响**：四层追溯链（p# → paper_summaries → search_log → sciverse_skill_log）在这一环断裂；读者按文献表核对将得到完全不同的论文

### 🟠 P1-1：CI"125 passed"与实际不符（测试收集缺口）

- `tests/test_core.py`（10 个 `_test_*` 函数：run_dir 隔离 / extract_json_object / discovery 确定性 / seed 复现 / classical_models / llm_prune_focus）与 `tests/test_search_isolation.py`（main() 风格）**不被 pytest 收集**（函数名非 `test_` 前缀）
- **实测证据**：`pytest --collect-only -q` = 125 项，全部来自 6 个文件：test_search(35) + test_discovery(30) + test_extractor(26) + test_config(24) + test_parser_engine(7) + test_budget_resume(3)——**test_core 与 test_search_isolation 一项未跑**
- 且这两个文件**直接运行也失败**：`ModuleNotFoundError: No module named 'utils'` / `UnicodeEncodeError: 'gbk' codec can't encode '\u274c'`
- **影响**：`.github/workflows/ci.yml:60-61` 注释声称"125 项覆盖 config/discovery/extractor/search/parser_engine/core functions/budget resume/search isolation"与事实不符——**core functions 与 search isolation 的断言实际从未在 CI 执行**

### 🟠 P1-2：thermoelectric 检索日志与声称篇数严重不符

- `workspace/data/literature_cache/thermoelectric/search_log.jsonl` 仅 **6 行**，但 paper_summaries 声称 **209 篇**；对应 sciverse_skill_log 也仅 6 行（4.7KB）
- perovskite 的 `paper_summaries.md` 存在**未清洗 Python dict 残留**（`{'title': ...}` 原文呈现，无 DOI）
- 主案例 `search_log.jsonl` 208 行 vs 声称"546 篇次检索"（覆盖率仅 38%）；两份日志（search_log + sciverse_skill_log 149 行）合计 357 行，口径无法直接还原 546
- cathode（25 行日志/27 篇）、validation（26 行/85 篇）证据链正常

### 🟡 P2：次要项

| # | 事项 | 证据 |
|---|---|---|
| P2-1 | **LLM 引导表述与审计等级不符** | 方案文档 P56 称主案例"两次判断直接改变搜索方向"，但 `docs/REPRODUCIBILITY.md` §3.1 明确主案例 llm_guidance 为**事后回填审计（未影响采样）**，仅 mof_e2e_v4 主题是真实端到端（n_events=5）；主案例 hypotheses.json 的 llm_explanation 标注"LLM API 不可用"（启发式评分） |
| P2-2 | **统计结论样本量披露不对称** | F=9.909/p=0.0254 基于 8 点（含 5 个估计点）数据集，README §3.4 引用时未说明含估计点；独立 5 实测点 F 检验 p=0.158 **不显著**（`quantitative_validation_nico5.md` 已诚实披露）；31 条假设仅 7/31 validated |
| P2-3 | **pyproject.toml 与 requirements.txt 不同步** | pyproject 用 `>=` 宽松约束（requirements 用 `==` 精确锁定）；pyproject 缺 chromadb/sentence-transformers/FlagEmbedding；`pyproject.toml:36` mypy `python_version="3.10"` 与 CI 矩阵 3.11/3.12 不一致；pytest 未写入任何依赖清单 |
| P2-4 | **Dockerfile 装全量 ~3GB 依赖** | `Dockerfile:17` 安装完整 requirements.txt（含 chromadb/sentence-transformers/FlagEmbedding ~3GB），实际运行与 CI 只用精简集（requirements-test.txt）；`COPY . .` 会把 pi-agent.zip(5.6MB)/`_*.py`/docx 备份拷入镜像；容器以 root 运行、无 workspace volume 挂载 |
| P2-5 | **`.api_key` 4 条真实密钥** | `.gitignore:22` 与 `.dockerignore:6` 已覆盖，静态检查未发现历史泄露；但 README 标注为公开仓库，无法排除历史提交已入库——**建议轮换** |
| P2-6 | **根目录临时产物噪音** | 16 个 `_*.py` + 6 个 `_*.txt` + 3 个 `_方案说明文档*备份.docx` + 8 个 `.docx.bak-*`（均不入库，但工作区脏） |

---

## 四、验证过无问题的部分（避免误伤）

- ✅ 6 主题 PDF/tex/bib 三件套齐全，bib 条数 71/41/48/27/24/44 与 `初赛提交物整理.md` 完全一致
- ✅ 方案说明文档.docx（35,349B）正常；13 个 `.bak-*` + 3 个 `_*备份.docx` 与正本区分清晰且均被 .gitignore 覆盖
- ✅ 31 条假设（SPR-MOF×5 / MOFv4×5 / PVSK×5 / TE×5 / NMC×6 / SE×5）与各主题 hypotheses.json 实际总数一致
- ✅ 主题口径一致：4 个正式研究主题 / 6 个 tracked 主题 / 10 个覆盖主题，与 workspace/outputs 目录一一对应
- ✅ pytest 125 passed（当前被收集的测试集真实通过；问题仅在"未被收集的测试没跑"）
- ✅ validation 主题补跑产物（2026-08-16）数字自洽：total_explored=49 = 17+16+16+0+0，search_h0.iterations=6 = hypo_1.search_iterations=6，5 条假设均 insufficient_data 且未虚构数值
- ✅ 6 主题均有 cross_theme_connections.md（validation 为 2026-08-16 新增，格式与其他主题一致）
- ✅ 主案例 Gap/假设的 known_prior_work 均锚定具体 P#（2026-08-16 已修复 MOF-01/03/05 虚构问题，未见复发）
- ✅ 密钥管理：日志不泄露密钥（config_status 只输出"已配置/未配置"、llm.py 不打印 key、sciverse_mcp 审计参数脱敏）

---

## 五、修复优先级建议

| 优先级 | 事项 | 建议动作 |
|---|---|---|
| **P0** | Best Score 回填 + 重新生成 SP 清单 | 写脚本把 search_h*.json 的 best_score 回填进各主题 hypotheses.json → 重跑 `scripts/build_route_a_docs.py` + 重编译 route_a PDF |
| **P0** | 补写 SP 清单占位文本 | 用已核实的 known_prior_work / incremental_claim 字段回填（hypotheses 侧 2026-08-16 已补，SP 清单需重新生成） |
| **P0** | p# 编号错位修复 | 最大工程：重建 report.tex 的 cite 映射或修正 references.bib 条目，重编译 6 个 PDF；建议先做主案例 |
| **P1** | CI 收集缺口 | 给 test_core.py / test_search_isolation.py 加 `test_` 前缀 + 修模块路径（PYTHONPATH），或 CI 显式运行两文件；修正 ci.yml:60-61 注释 |
| **P1** | 检索日志对齐/如实标注 | thermoelectric/perovskite 的 paper_summaries 与日志对齐，或文档中降级表述为"部分检索日志留存" |
| **P2** | 文档/配置同步 | 方案 docx LLM 引导表述、README 统计口径（含估计点说明）、pyproject 与 requirements 对齐、Dockerfile 精简依赖 + 非 root + volume、密钥轮换 |

---

## 附：各维度扫描摘要

### 附 1 · 赛题要求对照（满足度）

| 考察点 | 状态 | 证据 |
|---|---|---|
| 初赛提交物（方案说明文档 + 技术路线） | ✅ 满足 | `方案说明文档.docx`（35KB，覆盖方向/科学问题/技术方案/预期路线/初步验证/仓库链接/披露/团队） |
| 基本任务 · 文献检索与筛选 | ✅ 满足 | `literature_agent/search.py`、Sciverse 三层接入、546 篇次检索（日志口径见 P1-2） |
| 基本任务 · 知识抽取 | ✅ 满足 | `extractor.py` 四路径、五字段 schema、数值正则校验 |
| 基本任务 · Research Gap 识别 | ✅ 满足 | 主案例 10 项 Gap（含置信度/严重程度/证据/验证方案） |
| 基本任务 · 调研报告生成 | ✅ 满足 | 六章结构 + LaTeX 编译链路（数量对应，语义问题见 P0-3） |
| 路线 A · 搜索/优化算法 | ✅ 满足 | 贝叶斯（RBF-GP+MLE+UCB）/ MCTS / 符号回归 / 遗传算法 |
| 路线 A · LLM 深度融合 | ⚠️ 部分满足 | 仅 mof_e2e_v4 是真实端到端；主案例为事后回填（见 P2-1） |
| 路线 A · 可信性/新颖性 | ✅ 满足 | 四层证据追溯、F 检验/LOOCV/bootstrap/meta-analysis、MP/OQMD/NOMAD/hMOF 交叉验证、随机参照系 |
| 路线 A · 领域理解/可解释性 | ✅ 满足 | ROUTE_A_EXPLANATION 8 章、Vegard/Slack 失效机制解释 |
| 合规 · 外部资源披露 | ✅ 满足 | 13 项资源三层交叉引用（docs/EXTERNAL_RESOURCES.md + resource_registry.py + @external 注释） |
| 合规 · 密钥管理 | ✅ 满足 | .api_key 不入库；风险项见 P2-5 |

### 附 2 · 提交物完整性（逐项核实）

| 提交物 | 状态 | 说明 |
|---|---|---|
| ① 方案说明文档.docx | ✅ 齐全 | 35,349B，正本无后缀；备份均带 `.bak-*` 或 `_` 前缀 |
| ② pi-agent.zip（源码） | ✅ 齐全 | 5.6MB；pi_agent(12) / literature_agent(14) / utils(4) / scripts / tests(8) / main.py / requirements.txt / docs 均在；`.api_key` 被 gitignore 覆盖 |
| ③ 文献调研报告/ | ✅ 齐全 | 6 主题三件套 + 编译模板；bib 条数 71/41/48/27/24/44 与文档一致（语义问题见 P0-3） |
| ④ 路线A_构效关系清单/ | ⚠️ 部分 | 6 files 齐全，但 Best Score 全 0.000 + 占位文本（见 P0-1/P0-2） |
| workspace/outputs tracked 6 主题 | ⚠️ 部分 | 5 主题 discovery 产物齐全；validation 已于 2026-08-16 补跑齐全 |

### 附 3 · 代码工程质量要点

- ✅ 无 TODO/FIXME 残留；无裸 `except:`；路径统一走 `utils/config.py` 相对路径
- ✅ requirements.txt 全 `==` 精确锁定；requirements-test.txt 精简合理（CI 可避开 ~3GB ML 依赖）
- ✅ CI 存在（Python 3.11+3.12 矩阵、pytest 硬闸门、ruff 软检查）；main.py 参数/种子/run-dir 隔离完整
- ⚠️ 需改进：测试收集缺口（P1-1）、pyproject 不同步（P2-3）、Dockerfile 膨胀（P2-4）、pi_agent 核心路径 pytest 覆盖几乎为空（llm.py 重试 / agent.py 主循环 / tools.py 23 工具 / state_machine.py / MCTS / MP validator 均无测试）

---

*本报告由只读扫描 + 人工复核生成；未修改任何产物（除 CROSS_THEME_REPORT.md 两处"待生成"→"已生成"同步）。*
