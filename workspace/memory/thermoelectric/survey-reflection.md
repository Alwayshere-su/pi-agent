# 运行反思日志（覆盖）

## 2026-08-11 量化验证阶段完成

### 结果是否符合假设？
- **部分符合**：预期 model_comparison 能证明某些假设优于经典模型，但实际得到的是**诚实的负结果**——
  跨材料温度-ZT 单变量建模 R²≈0.29、F 检验不显著。这比"人为构造显著结果"更有科学价值：
  ZT 确实无法用单一温度变量预测（印证 Gap 1），需要多描述符。
- **符合预期**：idx 3（共振掺杂 Seebeck）数据不足——摘要级文献确实缺 Seebeck 数值；外部数据库
  validate 全部 inconclusive（MP/OQMD 无 ZT 数据）。

### 新学到的东西
1. 符号回归 R²（0.79）远高于多项式（0.29），但 9-12 参数复杂表达式 = 过拟合——
   **高 R² 不等于好模型**，参数数/样本数比才是关键。
2. 热电量化建模的硬约束：文献摘要只有峰值 ZT，几乎没有温度曲线/Seebeck 数值；
   知识图谱数值表只能靠"同体系不同论文"拼凑（数据异质性已如实标注）。
3. cross_theme 连接中热电与正极材料共享「稳定性」权衡、与 MOF 共享「纳米复合」策略——
   跨领域迁移的"性能-稳定性"通用描述符值得下一轮探索。

### 下一步策略调整
- 下轮聚焦：① 解析 2-3 篇关键论文全文（TE146/TE040/TE113）提取变温 Seebeck/热导率曲线，
  补齐 idx 3 数据；② 构建多描述符 ZT 回归（T + Eg + m* + vs），检验 R² 是否显著提升；
  ③ 尝试 NOMAD 输运数据或热电专项数据库替代 MP/OQMD。
- 量化数值表 7.1-7.6 可作为多轮迭代的共享数据资产，下次直接复用。
