# 问题定义文档 — 材料科学文献驱动的构效关系自主发现

> GOAI 赛道三「前沿探索 AI for Research」· 开放探索赛题
> 项目：Pi-Agent（文献驱动科学发现智能体）· 初赛提交版 2026-08-01

---

## 1. 看见了什么问题

### 1.1 领域背景

金属有机框架（MOF）材料是 CO₂ 捕获领域最具潜力的材料家族之一。该领域在过去二十年产出数万篇论文，但存在三个公认的结构性问题：

1. **知识碎片化**：吸附容量、选择性、吸附热（Qst）等关键性能分散在上千篇独立论文中，缺乏统一的结构化整理；
2. **结论矛盾**：同一材料在不同合成路线下性能数值差异巨大（如 Ni-MOF-74 的 CO₂ 容量报道值为 3.99–8.29 mmol/g），材料-工艺-性能三者关系远未厘清；
3. **权衡关系未被刻画**：高容量、高选择性、低再生能耗三者之间存在公认的 trade-off，但 Pareto 前沿的形状和驱动因素（如 Qst 的"甜点区间"）没有定量结论。

### 1.2 为什么 AI 可以介入

文献驱动的科学发现是一条「检索 → 阅读 → 抽取 → 组织 → 推理 → 验证」的流水线，恰好可以被 LLM Agent 端到端接管：

- **检索与阅读可自动化**：arXiv 与科学引文数据库（Sciverse）均有 API，可程序化检索、解析、抽取；
- **知识组织可由 Agent 自主完成**：Agent 可以自行撰写知识图谱（材料/性质/数值/关系）、识别研究空白（Gap）、生成可检验假设——而不是依赖人工构造的结构化模板（本项目已移除 JSON 知识图谱方案，改由 Agent 直接撰写 Markdown 知识图谱，效果更好）；
- **搜索与验证可闭环**：在文献数值驱动的贝叶斯优化/MCTS 搜索空间上，Agent 可自主探索材料-参数组合，并用外部数据库交叉验证。

### 1.3 切片选择

本项目不试图解决"设计下一代 CO₂ 捕获材料"这一终极问题，而是聚焦比赛周期内可探索、可验证的局部：

> **切片**：能否让一个 Agent 在有限时间预算（600s）内，自主完成「文献调研 → 知识图谱构建 → Gap 识别 → 构效关系发现」全流程，并产出可被领域专家理解、可追溯证据的发现信号？

该切片保留了真实科学问题的核心矛盾（容量-选择性-再生能权衡、水稳定性机理矛盾），又限定在单主题（MOF/CO₂ 捕获）内，保证可完成、可检查。

---

## 2. 如何定义探索环境

### 2.1 固定部分（环境规则）

| 组件 | 固定内容 |
|------|---------|
| 文献源 | arXiv + Sciverse 学术检索 API（arXiv 为开放获取，Sciverse 需 API Key） |
| 解析管线 | `literature_agent/parser.py`：双引擎 PDF 解析（MinerU 远程 + markitdown_utils 本地回退），自动抽取材料/性质/方法 |
| 摘要整理 | `extract_knowledge`：把检索结果整理为可读的 `paper_summaries.md` |
| 搜索算法 | 贝叶斯优化（加权 k-NN 代理 + UCB）与 MCTS，见 `literature_agent/discovery.py` |
| 证据打分 | 材料覆盖率 + 材料×性质共现 + 数值接近度（IQR 稳健搜索空间） |
| 预算 | 时间预算（默认 600s，可用 `--budget` 调整），触发强制收尾 |

    
**PDF 解析引擎策略**：解析管线采用双引擎设计——MinerU 作为推荐的解析引擎（MinerU Cloud v1 为公开 API，无需 API Key；另有本地服务与 pip 包两种模式），对中文论文和复杂表格/公式有更好的解析质量；markitdown_utils 作为本地回退引擎，离线免费且结果完全可复现。MinerU 不可用时自动回退到 markitdown_utils，不影响解析管线正常运行，但解析质量（尤其表格和公式）可能略有差异。回退策略对可复现性的影响：markitdown_utils 本地引擎的解析结果完全确定可复现；MinerU 远程引擎受网络状况和 API 配额影响，建议在需要精确复现的实验中使用本地引擎。
### 2.2 可探索部分（Agent 决策空间）

- 检索策略：检索词组合、检索轮次、补检索时机（本轮 Agent 在首轮后自主补充"水稳定性机理"检索，直接解决了 Gap 2 的核心矛盾）；
- 知识图谱结构：Agent 自行决定 Markdown 图谱的组织方式（材料体系/构效关系/矛盾/标杆值）；
- Gap 识别：哪些矛盾值得深挖、优先级如何排序；
- 假设生成：3–5 个可检验构效关系假设（材料、性质、预期关系、置信度）；
- 搜索空间与参数：`property_value / composition_x / temperature` 的搜索范围（由文献数值 IQR 自动界定）。

### 2.3 反馈机制（Agent 如何迭代）

反馈来自**文献证据本身**而非单一最终分数：

1. 每次工具调用的即时反馈（检索结果、文件读写结果、打分结果）；
2. `run_discovery_search` 返回证据索引统计（材料数、文献数值数、最优打分）；
3. `validate_discovery` 返回外部数据库（Materials Project / OQMD / NOMAD）的匹配结果；
4. 跨轮记忆：`MEMORY.md` + 运行反思（`survey-reflection.md`）让下一轮直接继承上一轮结论（本轮 Agent 在第二轮补充检索后把 Gap 2 置信度从 0.8x 上调至 0.92，把假设 4 置信度改为 0.86）。

---

## 3. 什么算发现信号

发现信号**在运行前定义**，正结果、负结果、异常、反例四类都算信号，均可被领域专家理解：

| 类型 | 定义 | 本轮实例 |
|------|------|---------|
| **正结果** | 有文献证据支撑、置信度 ≥ 0.7、可追溯论文 ID 的构效关系 | Qst 甜点区假设（29–40 kJ/mol，置信 0.89）；双金属 Ni:Co≈1:1 协同（0.82）；水 RH 双分支假设（0.86） |
| **负结果** | 被明确检验但未获支持的假设/方向 | Materials Project / OQMD 对 MOF 气体吸附性质的数据库覆盖为 0——本身升级为方法论发现（Gap 4 的延伸证据） |
| **异常** | 文献数值或结论之间的矛盾、不一致 | Ni-MOF-74 容量 3.99 vs 8.29 mmol/g 的矛盾区间；水-CO₂ 竞争/协同吸附的机理矛盾（Gap 2） |
| **反例** | 与主流假设相反的证据 | 胺功能化 MOF 在低湿度下容量反升（"水促进"分支），与"水总是有害"的朴素假设相反 |

**证据链要求**：每个发现必须带论文 ID（p#）与 DOI 可追溯；知识图谱中每条构效关系标注证据来源（本轮 R1–R11 均带 p# 引用）。

---

## 4. 最小参照系是什么

为了排除"只是随机运气"，本项目定义两层参照系：

1. **随机探索参照系（primary）**：在同一证据索引（`knowledge_graph.md`/`paper_summaries.md`）上，以**相同评估预算**公平对比两类搜索策略——复现 Agent 的贝叶斯搜索（10 次初始随机 + N 轮 UCB 采集）与同预算随机均匀采样，跨 10 个随机种子比较每假设最优打分的中位数。若贝叶斯中位数显著高于随机（差 > 0.01），发现信号可归因于搜索策略而非运气；否则为 parity（信号来自文献证据结构本身）。
   - 复现：`python scripts/baseline_random_search.py --iterations 40 --seeds 10`，输出 `workspace/outputs/literature_survey/discovery/baseline_random.json`（同预算公平对比，实现见复赛包）。
   - 本轮（600s 测试主题）结果：4 条假设全部 parity（|diff| ≤ 0.001，wins 6/10），如实记录为负结果——当前瓶颈在打分函数区分度而非搜索算法。
2. **平凡解参照系（secondary）**：仅完成阶段一（检索+摘要+报告），不做假设生成与搜索。此时"发现"输出为空——验证发现信号确实来自阶段二的探索行为而非文献本身自带结论。

---

## 5. 开源计划与边界

### 5.1 开源计划

- 进入复赛/决赛后，将以 GitHub 公开仓库形式提交可运行环境（`pi_agent/` + `literature_agent/` + 入口 `main.py`），包含依赖清单、配置方法、随机种子说明与复现步骤；
- 探索日志、运行反思、知识图谱等产物以 Markdown/JSON 随仓库开放（敏感 API Key 除外）。

### 5.2 边界与合规

- **商业 API 披露**：推理使用 DeepSeek API（模型 `deepseek-v4-flash`）；文献检索使用 Sciverse API；二者均为商业闭源服务，调用环节集中在 `utils/config.py` 与 `literature_agent/search.py`，费用随调用量线性增长（600s 单轮约数十次 LLM 调用）。**替代方案**：LLM 可替换为任意 OpenAI 兼容端点（`DEEPSEEK_BASE_URL`/`DEEPSEEK_MODEL` 环境变量）；检索可替换为纯 arXiv 开放接口（零成本）。可复现性影响：LLM 采样存在随机性，但搜索打分由文献数值确定，Agent 结论可被领域专家独立核验。
- **Sciverse 接入方式（如实说明）**：项目以 **REST API 直连为主**（`literature_agent/sciverse_mcp.py` 的 `SciverseSearcherRestAdapter`，通过 `SCIVERSE_API_KEY` 调用 `https://api.sciverse.space`），并保留两个可选扩展接入方式：**JSON-RPC MCP 适配层**（设置 `SCIVERSE_MCP_URL` 后，以自研轻量 JSON-RPC 2.0 HTTP 客户端向第三方 MCP server 端点发送 `tools/call`——注意这是 MCP 客户端适配层，不是官方 MCP SDK/配置，也不是 MCP server）；**外部 Skill 脚本**（设置 `SCIVERSE_SKILL_PATH` 后，通过 `subprocess` 调用外部 Python 脚本，stdin/stdout JSON 通信）。`create_sciverse_adapter()` 按 MCP > Skill > REST 优先级自动检测并逐级降级，全部不可用时回退到纯 arXiv 检索；`LiteratureSearcher.available_sources` 属性会标注当前实际激活的接入模式（如 `sciverse(rest)`）。本轮全部历史调用的审计记录 `adapter_mode` 均为 `rest`。
- **审计证据链**：无论实际接入模式（默认 REST 直连，可选 MCP/Skill 扩展），每次 Sciverse API 调用均生成标准化审计记录。每条记录包含唯一调用 ID、中国标准时间戳、调用参数 SHA256 哈希（前 16 位）、结果摘要等字段，持久化到 `workspace/logs/sciverse_skill_log.jsonl`。从最终报告中的任何一条 Gap/假设中的论文引用，均可通过标题或 DOI 关联到 `paper_summaries.md` 中的文献条目，再通过 `search_log.jsonl` 中的检索查询关联到具体的 API 调用记录，形成完整的端到端审计链条。审计机制详细说明见 `workspace/outputs/literature_survey/audit_trail.md`。
- **数据来源与授权**：arXiv 摘要/全文为开放获取（CC 授权）；Sciverse 检索结果仅取标题+摘要用于内部调研，不对外再分发；文献缓存存于 `workspace/data/literature_cache/`（已 gitignore）。
- **密钥管理**：API Key 存放于 `.api_key`（已加入 `.gitignore`），不进入版本库。

---

## 6. 预期评审视角自查

| 自检问题 | 本项目回答 |
|---------|-----------|
| 去掉 AI，这个问题还成立吗？ | 成立——MOF 构效关系的文献矛盾与 Pareto 权衡是真实的材料学问题 |
| Agent 行动会改变后续观察和决策吗？ | 会——检索策略、补检索、Gap 排序直接决定后续假设与搜索方向 |
| 反馈足以让 Agent 迭代吗？ | 是——证据打分、外部验证、跨轮记忆构成闭环 |
| 发现信号能被领域专家理解吗？ | 能——Qst 甜点区、双金属协同、水 RH 分支均为可检验的科学表述 |
| 最小参照系能排除随机运气吗？ | 能——随机探索参照系 + 平凡解参照系双重对照 |
| 可能的数据泄露/幻觉风险？ | 已控制——结论必须带论文 ID 证据链；外部数据库零覆盖被如实记录为负结果 |
| 失败结果能改进问题定义吗？ | 能——MP/OQMD 零覆盖已转化为"数据库覆盖空白"这一新 Gap（Gap 4/7） |

---

*本文件对应初赛提交要求（问题定义文档，≤4 页）。复赛将补充：最小可运行环境、完整运行日志、参照系实现与复现说明。*
